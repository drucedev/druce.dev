{
  "name": "druce.dev",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "preview": "bun run build && cd dist && bunx serve"
  },
  "type": "module"
}
