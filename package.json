{
  "name": "druce.dev",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && bun run scripts/size-check.ts",
    "preview": "bun run build && bunx serve dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5",
    "vite": "^8"
  },
  "dependencies": {}
}
